export type Todo = {
  title: string;
  description: string;
  completed: string;
  id: string;
  userId: string;
  created_at: Date;
};

export type User = {
  firstName: string;
  lastName: string;
  email: string;
  id: string;
};
