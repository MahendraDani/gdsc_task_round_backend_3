export type Todo = {
  title: string;
  description: string;
  completed: string;
  id: string;
  userId: string;
};

export type User = {
  firstName: string;
  lastName: string;
  email: string;
  id: string;
};
